import { Link} from "react-router-dom";

const Home = () => {
    return (
        <>
            <button type = "button">
                <Link to="/Contacts">Contacts</Link>
            </button>
        
        </>
    )
};

export default Home;